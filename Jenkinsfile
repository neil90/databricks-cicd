pipeline {
         agent any
         stages {
                 stage('One Echo') {
                 steps {
                     echo 'Hi, this is Neil'
                 }
                 }
                 stage('Two ls') {
                 steps {
                    sh 'ls -l'
            }
        }
    }
}